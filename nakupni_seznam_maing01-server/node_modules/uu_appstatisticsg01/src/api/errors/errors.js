const { UseCaseError } = require("uu_appg01_core-appserver");
// template: errors
// templateVersion: 0.1.1
//@@viewOn:revision
// coded: Hrynkiv Andriy (5890-4974-1)
// reviewed:
//@@viewOff:revision
//@@viewOn:constants
class AppStatisticsError extends UseCaseError {
  static get ERROR_PREFIX() {
    return "uu_app_statisticsg01/";
  }

  // TODO: need to refactor, now it's working with incorrect exceptions structure
  constructor(uuAppErrorMap, cause = null) {
    const { dtoOut, paramMap = {} } = uuAppErrorMap;
    if (paramMap instanceof Error) {
      cause = paramMap;
    }
    super({ dtoOut, paramMap, status: 400 }, cause);
  }
}

module.exports = AppStatisticsError;
